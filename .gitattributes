# Normaliza fim de linha: LF no repositório, nativo na cópia de trabalho.
# Evita que o site quebre ao ser clonado ou publicado em Linux.
* text=auto eol=lf

# Binários — nunca converter
*.jpg binary
*.png binary
*.webp binary
